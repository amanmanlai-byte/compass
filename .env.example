# AI Chat Web App - 环境变量配置

## 必填变量

| 变量名 | 说明 | 获取方式 |
|--------|------|----------|
| `DATABASE_URL` | 数据库连接 URL | SQLite: `file:./dev.db` / PostgreSQL: `postgresql://user:pass@localhost:5432/ai-chat` |
| `NEXTAUTH_SECRET` | NextAuth.js 加密密钥 | 运行 `openssl rand -hex 32` 生成 |
| `ENCRYPTION_KEY` | API Key 加密密钥（AES-256-GCM） | 运行 `openssl rand -hex 32` 生成 |

## OAuth 认证（可选）

| 变量名 | 说明 | 获取方式 |
|--------|------|----------|
| `GOOGLE_CLIENT_ID` | Google OAuth 客户端 ID | [Google Cloud Console](https://console.cloud.google.com/apis/credentials) |
| `GOOGLE_CLIENT_SECRET` | Google OAuth 客户端密钥 | 同上 |

### 配置 Google OAuth 步骤
1. 访问 [Google Cloud Console](https://console.cloud.google.com/apis/credentials)
2. 创建 OAuth 2.0 客户端 ID（Web 应用类型）
3. 添加授权重定向 URI：`http://localhost:3000/api/auth/callback/google`
4. 复制 Client ID 和 Client Secret 到 `.env`

## AI 提供商 API Key

所有 AI 提供商的 API Key 均通过 Web UI 在 **设置 > 模型与 API Key** 页面中配置，**不要**写在 `.env` 文件中。系统会使用 `ENCRYPTION_KEY` 对 Key 进行 AES-256-GCM 加密后存入数据库。

支持的 Provider 列表：
- OpenAI - https://platform.openai.com/api-keys
- Anthropic - https://console.anthropic.com/keys
- Google (Gemini) - https://aistudio.google.com/apikey
- DeepSeek - https://platform.deepseek.com/api_keys
- Mistral - https://console.mistral.ai/api-keys
- Groq - https://console.groq.com/keys
- xAI (Grok) - https://console.x.ai
- Moonshot (Kimi) - https://platform.moonshot.cn/console/api-keys
- 通义千问 (Qwen) - https://bailian.console.aliyun.com/
- 百川 AI - https://platform.baichuan-ai.com/console/apikey
- 零一万物 (Yi) - https://platform.lingyiwanwu.com/apikeys
- 智谱 AI (GLM) - https://open.bigmodel.cn/usercenter/apikeys
- Cohere - https://dashboard.cohere.com/api-keys
- Perplexity - https://www.perplexity.ai/settings/api
- Together AI - https://api.together.xyz/settings/api-keys

## 示例 `.env` 文件

```env
DATABASE_URL="file:./dev.db"
NEXTAUTH_SECRET="your-generated-secret-here"
NEXTAUTH_URL="http://localhost:3000"
ENCRYPTION_KEY="your-encryption-key-here"

# Google OAuth (optional)
# GOOGLE_CLIENT_ID="your-client-id"
# GOOGLE_CLIENT_SECRET="your-client-secret"
```
